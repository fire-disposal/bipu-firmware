// 可复用 UI 组件示例骨架

void widget_status_bar(const char* text) {
    // 绘制状态栏
}

void widget_textbox(const char* text) {
    // 绘制多行文本框，支持中文换行
}

void widget_list(const char** items, int count, int selected) {
    // 绘制选择列表
}